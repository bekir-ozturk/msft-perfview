<UserControl
    x:Class="PerfView.TimelineView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:src="clr-namespace:PerfView"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel x:Name="DataView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <src:TimelineSummaryCanvas
                x:Name="SummaryCanvas"
                DockPanel.Dock="Top"
                Height="90"
                Background="White"
                />
            <ScrollBar DockPanel.Dock="Bottom" Orientation="Horizontal" Minimum="0" Maximum="20000" ViewportSize="5000"/>
            <src:TimelineFocusCanvas x:Name="FocusCanvas" Background="White"/>
        </DockPanel>
        <Label Name="InfoLabel" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" >Failed to load</Label>
    </Grid>
</UserControl>
